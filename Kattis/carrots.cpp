#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n,p;
    cin >> n >> p;
    vector <string> s(n);
    for (int i=0; i<n; i++) {
        cin >> s[i];
    }
    cout << p << endl;
    return 0;
}
